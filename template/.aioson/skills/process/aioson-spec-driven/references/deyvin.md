# Streamlined Reference — @deyvin

Activation-only recovery remains lightweight and does not load this process skill further.

For concrete continuation, resolve the feature and read only the reviewed PRD, approved implementation plan, `dev-state.md`, and the relevant code/tests. Resume the next incomplete vertical phase. Use `approval-gates.md` only when the action crosses plan approval or final QA.

Do not reconstruct or require a legacy spec pack. If the PRD and plan contradict observable behavior, stop and route the specific decision to `@sheldon` or `@planner`; otherwise continue the small validated slice.
