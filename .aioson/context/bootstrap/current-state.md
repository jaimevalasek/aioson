---
updated_at: "2026-05-14T22:30:00.000Z"
generated_at: "2026-05-14T22:30:00.000Z"
source: "Autonomy/orchestration analysis and planning session"
---

# Current State

## What the system already has

These capabilities were confirmed during this analysis:

- `@dev`, `@qa`, and `@committer` prompts already contain CLI-aware behavior
- `workflow:next --complete` is the real stage transition primitive
- `workflow:next --auto-heal` already exists
- `workflow:heal` already exists as manual fallback
- handoff generation already happens in workflow completion flow
- technical gates already block broken handoffs
- handoff contracts already block incomplete stage exits
- `commit:prepare` already exists and is the expected pre-commit path
- `workflow:harden` already exists
- test briefing and path guard are already injected by the motor
- machine-readable autonomy policy layer now exists via `.aioson/config/autonomy-protocol.json`
- official workflow agents now have capability manifests in `.aioson/agents/*.manifest.json`
- `agent:prompt`, `workflow:next`, and `workflow:execute` now resolve an effective autonomy mode
- workflow completion now dual-writes `last-handoff.json` and `handoff-protocol.json`
- handoff protocol validation now exists in warning-first mode
- `workflow:next --status` now exposes active stage, queued next stage, contract readiness, pending gate, autonomy mode, artifacts, and last handoff data
- `workflow:next --suggest` now returns a deterministic next command based on workflow state and handoff contract readiness
- `commit:prepare` now supports an explicit agent-safe/headless path and blocks non-interactive staging ambiguity with a structured error
- `workflow:execute` now seeds/resumes feature workflow state, predicts blockers in dry-run, and advances one or more valid checkpoints through `workflow:next`
- project-mode handoff gates now use `.aioson/context/spec.md` when present and no longer fail just because a workflow stage has no feature slug
- `@ux-ui`, `@pm`, and `@orchestrator` prompts/manifests are now aligned with the current runtime contracts instead of outdated harness assumptions
- `parallel:init` now creates a machine-readable parallel workspace baseline with `workspace.manifest.json`, `ownership-map.json`, and `merge-plan.json`
- `parallel:assign` now writes explicit lane ownership and merge metadata into both lane status files and machine-readable artifacts
- `parallel:assign` now preserves manual lane dependencies declared in the lane status files when rebuilding machine-readable artifacts
- `parallel:status` now reports machine-file health, ownership conflicts, dependency blockers, dependency order violations, and stale machine-readable artifacts
- `parallel:doctor` now validates stale machine-readable artifacts, ownership conflicts, invalid lane dependencies, blocked dependencies, and merge-order violations
- `parallel:doctor --fix` now reconstructs stale machine-readable parallel artifacts in addition to restoring missing shared and lane status files
- `parallel:merge` now consumes `merge-plan.json` and executes a deterministic merge only when every lane is structurally ready
- merged lanes now persist as `status: merged`, and `parallel:status` reports merged lanes explicitly
- lane status files, `workspace.manifest.json`, and `ownership-map.json` now carry explicit `write_paths` declarations per lane
- `parallel:status` and `parallel:doctor` now report write-scope coverage gaps, invalid `write_paths` patterns, and overlapping file ownership across lanes
- `parallel:guard` now validates whether a given lane is allowed to write specific project paths before execution starts
- `parallel:merge` now also blocks when declared `write_paths` overlap or contain invalid patterns
- `@sheldon` now generates `harness-contract.json` for MEDIUM features as part of RF-05, populating binary criteria from PRD ACs (full procedure in `.aioson/docs/sheldon/harness-contract.md`)
- `@qa` now recommends `@validator` in the QA report when `.aioson/plans/{slug}/harness-contract.json` exists for the active feature, gating `feature:close` via `progress.json.ready_for_done_gate`
- `aioson agent:prompt --headless --output=<file>` now generates an agent prompt without launching an editor and without registering a live session, suitable for CI runners and external LLM execution
- `aioson harness:validate` is now a router: when `.aioson/plans/{slug}/last-validator-output.json` exists it consumes it via `harness:apply-validation`; otherwise it generates the @validator prompt headless for external LLM execution
- `aioson harness:apply-validation . --slug=<slug> [--input=<path>]` consumes @validator JSON output and translates `results[].reason` of the first failure into `progress.json.last_error` (format `"<id>: <reason>"`), records via the circuit breaker, archives the input under `validator-runs/<timestamp>.json` after PASS or FAIL
- `harness:validate` now sets `progress.json.status = 'waiting_validation'` after generating the validator prompt, and `harness:apply-validation` resets it to `'in_progress'` after consuming output (preserving `circuit_open` when error_streak limit fires)
- `aioson workflow:next` now auto-routes to `@validator` (as a detour, returning to the original `state.next` after validator completes) when the active feature has `harness-contract.json` and `progress.status === 'waiting_validation'`. Explicit `--agent=…` overrides preserve user intent; without contract or in MICRO/SMALL the routing is a no-op
- `aioson feature:close --verdict=PASS` now enforces the harness done gate: if `harness-contract.json` exists and `progress.json.ready_for_done_gate !== true`, closure is blocked with the pending criterion ID/reason. `--force` bypasses with an explicit BYPASS audit trail entry. `--verdict=FAIL` skips the gate (QA already rejected). Without contract, behavior is unchanged.
- `@copywriter` now supports Mode 6 (Campaign Package), G0 genome resolution rule (folder vs single-file), G2.5 master copywriter selection (8 schools), R0.5 avatar gate, alternative structures (Tríade, KSTK, CPGC ads), LightCopy 4-style voice selection, plus new marketing references (headline-matrix, cta-matrix, platform-constraints); `@product` routes `project_type=site` to `@copywriter` and `@ux-ui` Step 0.5 copy gate halts visual layout when no `copy-{slug}.md` exists.
- `aioson memory:reflect-prepare/commit` now provide deterministic in-harness reflection of `bootstrap/*.md`: a pure engine (`src/memory-reflect-engine.js`) classifies git diff against routes/models/contracts/volume signals, builds a manifest with snapshot hash and allowed paths at `.aioson/runtime/reflect-prompt.json`, and validates the agent's output (frontmatter intact, `generated_at` bumped, snapshot hash matches) before writing to `.aioson/context/bootstrap/`. Prompts for each target file are in `template/.aioson/templates/reflect-prompts/`.
- `autonomy-protocol.json` is now v1.1 with `tiers` (tier1_silent / tier2_notified / tier3_blocking) and `derived_from_tiers` per harness; `src/permissions-generator.js` materializes `.claude/settings.json`, `.codex/permissions.json`, `.gemini/permissions.toml`, and `.opencode/permissions.yaml` at install/update time. Tier3 (`git push`, `npm publish`, `cloud:publish:*`, etc.) is hard-rejected even if a tool lists it. Generator is idempotent and backs up the previous version under `.aioson/backups/{ts}/permissions/`. v1.0 protocols continue to work via legacy `shell_whitelist`/`aioson_whitelist` fallback.
- `aioson notify --level=info|warn|block --topic=<t> --message=<m>` is now a CLI command — visual wrapper over `runtime:emit` with prefixes ℹ/⚠/⛔ and exit code 2 on `level=block` so callers can halt for human input.
- Agents `@dev`, `@qa`, and `@deyvin` now declare capability `reflect_memory` (tier2_notified, trigger=post_agent_done) in their manifests, carry a "Memory reflection" section in their `.md` files that consumes `.aioson/runtime/reflect-prompt.json`, and surface a bootstrap-coverage advisory (`⚠ [bootstrap] coverage <N>/4`) at session start when `aioson memory:status` reports gaps. `deyvin.manifest.json` is new.
- `aioson workflow:next --complete=<agent>` and `aioson agent:done` now call `memory:reflect-prepare` as a best-effort hook; when the diff is relevant a manifest lands at `.aioson/runtime/reflect-prompt.json` and `workflow:next` prepends a `ℹ [memory] reflect-prompt.json pending` line to the next agent's activation prompt. Both hooks fail silently if anything errors — reflection never blocks the workflow.
- `.aioson/docs/autonomy-protocol.md` is now an on-demand doc describing the 3-tier permission model, the 4 native harness formats, the notify levels, backward-compat rules, and the procedure to add a new command to a tier.
- `aioson doctor` now runs 5 additional Living Memory checks at `severity: 'warning'` (`bootstrap_coverage`, `features_dir_present`, `claude_commands_present`, `version_drift`, `permissions_in_sync`); the `--fix` flag auto-creates `.aioson/context/features/`, restores missing `.claude/commands/aioson/agent/*` slashes from the template, and regenerates native harness permission files via `permissions-generator`. Bootstrap coverage and version drift are advisory (no auto-fix). Warnings surface in the diagnosis line but do not break `report.ok`, so existing tooling that gates on doctor's overall verdict keeps working. Hints are localised in en, pt-BR, es, and fr.
- Living Memory user documentation now lives at `docs/pt/living-memory/` (7 files: README index + memoria-viva concept + reflexao-in-harness pipeline + autonomy-contract 3-tiers + notificacoes-info + troubleshooting 11 receitas + diagramas ASCII), linked from the main `docs/pt/README.md`. ~1440 lines in pt-BR covering the entire feature for non-developer readers.
- `/discover` slash command is now registered across all four supported harnesses — `template/.claude/commands/aioson/agent/discover.md`, `template/.gemini/commands/aios-discover.toml`, and `template/OPENCODE.md`. `src/constants.js` extended so `aioson doctor` detects previously-invisible buckets (`agents/discover.md`, `gemini aios-discover.toml`, four representative Claude slashes); `doctor --fix` restores missing entries from template. `@discover` is registered in `AGENT_DEFINITIONS` with bootstrap outputs.
- `@neo` routing rules and `@sheldon` scope boundary now distinguish PRD-only analysis (sheldon) from code archaeology (deyvin) and structural review of implemented systems (architect); `@sheldon` carries an explicit ✅ in-scope / ❌ out-of-scope block and a refuse-and-redirect protocol for mis-routings. Workspace and template synced.

- Handoff contracts and workflow state now respect **feature-level classification**: `CONTRACTS.dev` and `CONTRACTS.qa` skip Gate C/D for MICRO features via `checkGateApproval(..., classification)` short-circuit; `resolveClassification` (in `src/handoff-contract.js`) reads `prd-{slug}.md` frontmatter before falling back to project-level classification; `loadOrCreateState` (in `src/commands/workflow-next.js`) detects feature-transition (existing.featureSlug ≠ active feature per `features.md`) and rebuilds state instead of inheriting stale completion records from a closed feature. New tests in `tests/handoff-contract-micro.test.js` (7 cases). Unblocks MICRO features inside MEDIUM projects (inception mode bug surfaced 2026-05-11 during `deyvin-density` handoff).

- Sub-task scout engine Phase 1 (core-engine) shipped 2026-05-13: `src/sub-task-engine.js` provides the contract layer (`buildPrompt`, `validateInput/Output/Config`, `enforceCaps`, `generateScoutId`, `loadConfig`, `defaultConfig`) consumed by the future `aioson scout:prep|validate|commit` CLI verbs and by `@deyvin`'s sub-task scout invocation block. Hand-rolled recursive JSON validator (~80 LOC, zero new deps). Schemas in `src/sub-task-schemas.js` enforce `parent_session_excerpt` (50-1000 chars, required for cold-load comprehension), Nautilus-pattern read-only sub-agent constraints in the prompt template, and strict `additionalProperties: false` everywhere. 51/51 tests in `tests/sub-task-engine.test.js`. Phase 2 (cli-verbs) and Phase 3 (wiring-and-lifecycle) queued.

- Sub-task scout Phase 2 (cli-verbs) shipped 2026-05-13: 3 new CLI commands `aioson scout:prep|validate|commit` (`src/commands/scout-*.js`), state file + file-lock helper at `src/sub-task-state.js` (`.aioson/runtime/scouts/.state.json` with `.lock` PID+ISO, stale 30s), template config `template/.aioson/config/scout-engine.json` (empty `{}` so defaults from engine apply on `aioson update`), 10 spawn-based integration tests covering ACs C1-C9. `src/cli.js` extended with the 3 new commands. `scope_globs` deferred to V2 (Node 18-21 has no built-in `fs.glob`); V1 supports `scope_paths` only, with directory paths expanding to one level of file children. Telemetry reuses `agent_events` table with `event_type='sub_task'` and `payload_json.action ∈ {prepared, validation_failed, retry_exhausted, committed, slow_completion, cap_exceeded}`. 10/10 CLI tests + 61/61 scout total. Full regression: 2238/2275 (same 37 pre-existing failures unchanged). Phase 3 (wiring-and-lifecycle) queued.

- Active-learning-loop feature (MEDIUM, in_progress 2026-05-14): full spec chain produced — PRD with Differentiation section (vs Anthropic Dreaming + Hermes + Auto Dream), Sheldon enrichment (sizing 15, Path B 6 phases, 10 pre-made decisions + 5 deferred), analyst requirements (3 new entities, 2 extended tables, 12 BRs, 15 ECs, 30 binary ACs), architect resolutions (DD-1..5 closed: CLI verb `context:load`, foreground 5s execution, SQLite row-lock, BM25, defer brain-merge to follow-up), 6 phase plans + 5 decision pointer files. Gate B approved. Implementation pending — first slice `telemetry-foundation` (Phase 1). Code not yet shipped.
- Active-learning-loop feature **fechada em 2026-05-14** (`aioson feature:close --slug=active-learning-loop --verdict=PASS`). Todas as 6 phases qa_approved (Phase 6 Gate D em 2026-05-14: 4/5 ACs covered + 1 deferred per user — AC-ALL-604 docs × 4 idiomas reduzido a pt + en pelo próprio usuário). Closure disparou o hook da Phase 5 — **inception self-eat consumado**: a feature usou o loop que ela mesma construiu para registrar seu próprio fechamento em `evolution_log` (1 entry `auto_distillation` com `feature_slug='active-learning-loop'`, state=complete, duration_ms=3). Archive: 10 arquivos movidos para `.aioson/context/done/active-learning-loop/` (architecture, prd, requirements, sheldon-enrichment, spec + 5 QA reports phase 2-6 + dossier). features.md flipped para `done`. Cumulative testing record: **112/112 deterministic tests verdes** (Phase 1-6 dev + Phase 2-6 QA suites). Post-closure docs pass via Sonnet 4.6: criadas `docs/pt/active-learning-loop/` (7 arquivos, 1526 linhas pt-BR — README + ativo-learning-loop + como-usar + comandos-cli + diagramas + doctor-checks + troubleshooting) e `docs/en/active-learning-loop/` (7 arquivos, 1526 linhas en — translation fiel) + cross-link em `docs/{pt,en}/README.md`. Total 3052 linhas de documentação user-facing em 2 idiomas (`es` e `fr` ficam como follow-up MICRO se demanda surgir). M-01 follow-up identificada: implementar merge inteligente em `.aioson/config/*.json` no installer (atinge autonomy-protocol, scout-engine, learning-loop — comportamento atual sobrescreve customizations do usuário em `aioson update`). M2 follow-up identificada: extender `pattern:detect` para aceitar `--feature=<slug>` (V1 da feature-close hook só dispara `learning:auto-promote`; `pattern:detect` exige `--squad` e não foi orquestrado).
- Active-learning-loop Phase 6 (inception-mirror-parity) shipped 2026-05-14 — **feature funcionalmente completa, awaiting @qa Gate D para closure**. 3 fixtures de inception: `tests/active-learning-loop-inception.test.js` simula 5 fechamentos de feature em tmpdir greenfield (installTemplate + 5 PRDs MEDIUM + emit rule_loaded para cada rule do template em cada feature + 5× runFeatureClose --verdict=PASS) e asserta que `runDoctor` retorna zero entries `living-memory:rule_staleness|learning_orphans|distillation_lag` com `ok=false`, com `distillation_lag.params = { closed:5, distillations:5, threshold:5 }` — **valida o Success Metric do PRD** "zero curation candidates não surfaceados após 5 features fechadas". `tests/inception-parity-active-learning-loop.test.js` (4/4) verifica greenfield install: `.aioson/config/learning-loop.json` com defaults canônicos + 3 placeholders `_archived/.gitkeep` em rules/brains/context/ + autonomy-protocol tier1 lista context:load+memory:search e tier2 lista memory:archive+memory:restore + 3 doctor checks emitidos com severity=warning em fresh install (EC-ALL-11 graceful) + 4 CLI verbs registrados em KNOWN_COMMANDS + 4 command modules required. `tests/active-learning-loop-wiring.test.js` (7/7) cobre AC-ALL-603 (checkLearningLoopTemplateParity zero issues hoje + reporta drift em template sintético com config/autonomy/placeholders incompletos) + AC-ALL-605 wiring audit estático: doctor.js wires 3 check functions + emite 3 check ids, feature-close.js require'a learning-loop-engine + invoca runDistillation + captura preArchiveClassification + require'a notify, runtime-store.js exporta appendContextLoadEvent + chama runLearningLoopMigration, 9 source modules Phase 1-5 existem em disco, agentes que mencionam verbs no workspace mencionam também no template (surgical parity). `src/commands/sync-agents-preflight.js` extendido com `checkLearningLoopTemplateParity(projectRoot)` que detecta 4 classes de drift (missing config/autonomy/placeholders + invalid JSON + missing keys + missing verbs em tier1/tier2). `main()` retorna exit 1 com mensagem específica `[sync:agents BLOCKED] Active Learning Loop template parity issues:` + emite telemetry `learning_loop_template_parity_violation`. Fix incidental em `src/installer.js`: whitelist para `.aioson/context/_archived/.gitkeep` (a regra `context-protected` bloqueava o placeholder; precedent: `.gitkeep` e `design-doc.md` já tinham whitelist). **Adaptação documentada do Phase 6 plan**: pedia paridade de `template/src/`, mas `template/src/` não existe by design (Phase 1 Agent Trail confirmou que `template/` mirrors apenas `.aioson/`) — parity check pivotou para os artefatos `.aioson/` reais do template. AC-ALL-604 (docs em 4 idiomas) deferred per usuário — Sonnet 4.6 pass após Phase 6 QA aprovar. Cumulative regression: **106/106 deterministic tests verdes** (Phase 1 10 + Phase 2 17 + Phase 2 QA 3 + Phase 3 18 + Phase 3 QA 4 + Phase 4 18 + Phase 4 QA 6 + Phase 5 12 + Phase 5 QA 6 + Phase 6 inception 1 + Phase 6 parity 4 + Phase 6 wiring 7). Feature awaiting @qa Gate D para AC-ALL-601..605 — após PASS, `aioson feature:close --slug=active-learning-loop --verdict=PASS` pode rodar e o hook da Phase 5 vai disparar a distillation final do próprio feature (inception self-eat).
- Active-learning-loop Phase 5 (feature-close-distillation-hook) shipped 2026-05-14: novo `src/learning-loop-engine.js` orquestra a pipeline de distillation. Exports: `runDistillation`, `readFeatureClassification`, `acquireDistillationLock`, `releaseLockWithSummary`, `recordDistillationFailed`, `withTimeout`. Lock acquisition via `BEGIN IMMEDIATE` transaction + `SELECT WHERE end_at IS NULL` + `INSERT` (DD-3 SQLite row-level, BEGIN IMMEDIATE escalates to write lock collapsing SELECT-then-INSERT race window). Execução foreground via `Promise.race(workPromise, timeoutPromise)` com 5s timeout (DD-2 — better-sqlite3 é síncrono, timeout protege orquestração contra trabalho externo). Lock release via `UPDATE evolution_log SET end_at = ?, payload_json = { state:'complete', promoted_count, review_count, skipped_count, merge_candidate_count:0 (DD-5 deferred), duration_ms } WHERE id = ?`. Failure path: entry separada `event_type='distillation_failed'` com `payload_json = { error_phase, error_message }`. Entry-id `evo-{event}-feature-{slug}-{ts}-{rand6}` via `crypto.randomBytes(3)` para resistir a colisão same-ms no PRIMARY KEY. `target_type='feature'` adicionado ao vocabulário (Phase 3 só usava rule/learning/brain; Phase 5 introduz feature como target porque distillation é per-feature). Engine usa `autoPromoteModule.runLearningAutoPromote` (require + dynamic access) ao invés de destructured import — permite monkey-patch em tests de failure injection. Hook em `src/commands/feature-close.js`: classification capturada PRE-archive em `preArchiveClassification` (descoberto bug durante teste: archive move `prd-{slug}.md` para `.aioson/context/done/{slug}/`; leitura post-archive retornava null e bypassava MICRO opt-out). Skip rules: `verdict !== 'PASS'` OR `featureClassification === 'MICRO'` (BR-ALL-11 / PMD-5) OR `--no-distill` flag. Tier-2 notify único per closure via `runNotify` in-process com `level=info, topic=learning-loop` (BR-ALL-06 — antes da mutação não se aplica porque distillation já mutou ao chegar no notify; notify é o announce do resumo). Best-effort silent: feature-close exit code preservado mesmo em failure. Template `.aioson/config/learning-loop.json` com config default per-projeto (enabled, skip_on_classification, execution_mode, lock_strategy, auto_promote_threshold, staleness_window_features_min, timeout_ms). i18n 4 locales (en/pt-BR/es/fr) com bloco `doctor.learning_loop` × 6 keys cada (distillation_complete, distillation_failed_silent, skipped_micro, skipped_no_distill, lock_held, notify_template = 24 strings novas). `tests/feature-close-distillation.test.js` 12/12 verde cobrindo AC-ALL-501..506 + variantes (--no-distill, verdict=FAIL, readFeatureClassification helper, withTimeout helper timeout/happy path). Cumulative regression: 88/88 deterministic tests verdes (Phase 1 10 + Phase 2 17 + Phase 2 QA 3 + Phase 3 18 + Phase 3 QA 4 + Phase 4 18 + Phase 4 QA 6 + Phase 5 12). Phase 5 awaiting @qa para AC-ALL-501..506 + Gate D. Restante: apenas Phase 6 (inception-mirror-parity) após Phase 5 ser aprovada.
- Active-learning-loop Phase 4 (doctor-curation-checks) shipped 2026-05-14: novo `src/learning-loop-doctor.js` com `computeStalenessThreshold(featureCloseDates: Date[]): number` (pure function — fórmula `N = max(5, ceil(avg_days_between_last_5_features / 7))`, projetos low-velocity esticam acima do mínimo 5), `readProjectClassification(targetDir)` (frontmatter `classification:` de `project.context.md`), `readClosedFeatures(targetDir)` (parser de pipe-table em `features.md` com filtro `status=done`), `listRuleSlugs(targetDir)` (enumera `.aioson/rules/*.md` excluindo `_archived/`), e 3 query helpers: `assessRuleStaleness({ db, targetDir, threshold, recentFeatureSlugs })` retorna rules sem `rule_loaded` event nas últimas N features (eventos com `feature_slug=NULL` contam como "loaded somewhere" per EC-ALL-02), `assessLearningOrphans({ db })` retorna learnings `status='promoted'` cuja rule não tem `rule_loaded` event posterior ao `updated_at` da promoção (match em `target_path` ou `target_slug`), `assessDistillationLag({ db, closedFeatures })` retorna features `status=done` em `features.md` sem entry `auto_distillation` em `evolution_log` (fires apenas quando ≥5 closed features). `src/doctor.js` ganhou wire de 3 novos checks ao nível `severity='warning'` em `runDoctor`. BR-ALL-11 (MICRO opt-out): quando `classification === 'MICRO'`, os 3 checks emitem `ok=true` com i18n key `*_skipped_micro`. EC-ALL-11 (fresh install): quando `openRuntimeDb` falha (sem aios.sqlite ainda), checks emitem `ok=true` (zero false positives em pristine state). Report inclui novo bloco `livingMemory.curation` com `classification, closedFeatureCount, stalenessThreshold, dbError`. Hint params incluem ready-to-copy CLI: `aioson memory:archive --id=rule:<slug> --reason="not loaded in last N features"`. i18n em 4 locales (en/pt-BR/es/fr) com bloco `doctor.living_memory` × 9 keys cada (`rule_staleness`, `_hint`, `_skipped_micro` × 3 checks = 9 keys × 4 idiomas = 36 strings). `tests/doctor-curation-checks.test.js` 18/18 verde cobrindo: pure threshold formula (4 testes), FS readers, AC-ALL-401..406, BR-ALL-11 MICRO opt-out, EC-ALL-11 fresh install, NULL feature_slug semantics (EC-ALL-02). Regressão: Phase 1-3 todas preservadas (70/70 cumulativo). Smoke test no workspace ativo: detectou corretamente 13 rules estagnadas + lag de 12 distillations (esperado — Phase 5 hook ainda não shipou). Phase 4 awaiting @qa para AC-ALL-401..406 + Gate D.
- Active-learning-loop Phase 3 (memory-archive-with-evolution-log) shipped 2026-05-14: `src/learning-loop-migration.js` extendido com Phase 3 steps (ALTER TABLE evolution_log ADD COLUMN × 9 — event_type, target_type, target_id, start_at, end_at, reason, actor, feature_slug, payload_json — idempotente via `PRAGMA table_info`) + 3 índices (`idx_evolution_log_target`, `idx_evolution_log_active` partial WHERE end_at IS NULL, `idx_evolution_log_feature`). **Migration runner ganhou `PRAGMA user_version=3` short-circuit** — chamadas subsequentes em DB já migrado retornam imediatamente (1000 chamadas em ~12ms; antes do short-circuit, ~150ms cumulative na pior carga Windows). Novo `src/learning-loop-archive.js` (~470 LOC sob orçamento 500) com helpers puros: `parseTargetId`, `normalizeKind`, resolvedores para rule/brain/learning, `findActiveEntry`, `listHistory`, `insertEvolutionEntry` (app-level CHECK), `setActiveEntryEndAt` (única UPDATE permitida — BR-ALL-02), `archiveTarget`, `restoreTarget`. Entry-id `evo-{event}-{type}-{id}-{ts}-{rand6}` com `crypto.randomBytes(3)` para resistir a colisão same-ms no PRIMARY KEY. Novos CLI verbs `aioson memory:archive [path] --id=<type>:<slug> --reason="<text>" [--feature] [--dry-run] [--json]` e `aioson memory:restore [path] --id=<type>:<slug> [--reason] [--feature] [--dry-run] [--json]` em `src/commands/memory-{archive,restore}.js`. Ambos: (1) bloqueiam invocação via hook (BR-ALL-01 — `AIOSON_RUNTIME_HOOK==='1'`); (2) emitem `notify --level=warn` antes de qualquer mutação FS/DB (BR-ALL-06 — chama `runNotify` in-process); (3) suportam `--dry-run` com zero side effects; (4) idempotentes (re-archive de target já arquivado retorna `already_archived`). Restore preserva history: cria novo `event_type='restored'` com novo `start_at`, mantém o `end_at` da entry archived anterior (PMD-10). Arquivos archived seguem convenção `_archived/{YYYY-MM-DD}/` com sufixo de colisão `-{seq}` (até 9999). Cross-volume `fs.rename` fallback via copy+unlink em EXDEV/EPERM. Para learnings: arquiva via DB flip (`status='archived'`) + snapshot JSON em `.aioson/context/_archived/{date}/{learning-id}.json`. Registrado em `src/cli.js` (KNOWN_COMMANDS + dispatch + help lines × 2). i18n keys novas em 4 locales (en/pt-BR/es/fr): `help_memory_archive`, `help_memory_restore`, `memory_archive.*` (10 keys) + `memory_restore.*` (9 keys). `template/.aioson/config/autonomy-protocol.json` ganhou `memory:archive` + `memory:restore` em `tier2_notified`. Placeholders `.gitkeep` em `template/.aioson/{rules,brains,context}/_archived/`. `tests/memory-archive.test.js` 18/18 verde cobrindo AC-ALL-301..306 (variantes rule/learning/brain), tier-2 hook block, tier-2 notify pre-mutation, append-only invariant (start_at + reason imutáveis), restore history preservation, integridade referencial via evolution_log, dry-run zero-side-effect (archive + restore), EC-ALL-04 idempotency, validation paths (missing_id, missing_reason, invalid_id, target_not_found, target_not_archived), findActiveEntry sanity e migration idempotency (15 colunas + 3 índices). Regressão: Phase 1 acceptance (10/10) + Phase 2 acceptance (17/17) + Phase 2 QA pin (3/3) — todos preservados. Flake conhecido: `tests/qa-telemetry-foundation.test.js#QA-PERF-01` é Windows-IO-sensitive (3 runs consecutivos = PASS/FAIL/PASS); standalone profile dentro do SLA (p99 94ms vs 100ms cap). Phase 3 awaiting @qa para AC-ALL-301..306 + Gate D.
- Active-learning-loop Phase 2 (memory-search-fts5) shipped 2026-05-14: `src/learning-loop-migration.js` extendido com FTS5 virtual table (`project_learnings_fts` external-content sobre `project_learnings`, columns title+evidence, tokenizer `unicode61 remove_diacritics 2`), 3 triggers (`project_learnings_ai/au/ad`) para sync transacional (BR-ALL-07), backfill INSERT guarded por count check, plus guard de existência da source table (preserva AC-ALL-103 v1.x compat). Novo `src/learning-loop-fts5.js` com helpers puros (`sanitizeFtsQuery`, `validateQuery`, `normalizeSurface`, `buildSearchSql`, `searchProjectLearnings`); sanitization refinada do DD-4 literal de "phrase-only" para **token-AND** (tokenize-on-whitespace, strip FTS5 operator chars per token `*()^:+-"`, wrap cada token em phrase quotes, AND across tokens) — mesmo envelope de segurança, UX "all keywords present anywhere", alcança 10/10 na fixture canônica. Novo CLI `aioson memory:search "<query>" [path] [--limit=5] [--surface=rules|learnings|all] [--include-archived] [--json]` (`src/commands/memory-search.js`); `target_type` e `target_id` derivados em SQL via CASE on `promoted_to` (schema real de `project_learnings` não tem essas colunas — plan-file estava impreciso, requirements § E1 é canônico). Registrado em `src/cli.js` e em `template/.aioson/config/autonomy-protocol.json` `tier1_silent`. 6 i18n keys novas × 4 locales. Fixture `tests/fixtures/memory-search-queries.json` (10 queries) + `tests/memory-search.test.js` (17 testes verdes) cobrindo AC-ALL-201..205 + EC-ALL-08 (special chars, length cap 500, empty/whitespace) + ORDER BY rank ASC + trigger sync (insert/update/delete) + surface filter (rules/learnings/all) + invalid surface + helper unit tests + idempotency. Phase 2 done; awaiting @qa Gate D para AC-ALL-201..205.
- Active-learning-loop DD-4 (FTS5 ranking) closed 2026-05-14 — extends `.aioson/plans/active-learning-loop/decision-search-ranking.md` from the prior @architect baseline (BM25 default) with implementation guardrails for @dev Phase 2: mandatory bind-parameter usage, phrase-query sanitization as default (escape `"` and wrap in quotes; FTS5 operators are opt-in V2), 500-char length cap (EC-ALL-08), snippet config `snippet(project_learnings_fts, -1, ...)`, fixed JSON output schema `{ok, query, result_count, results[]}` for dashboard, explicit cross-DD boundaries (search does NOT emit context_load events; Phase 5 distillation writes propagate to FTS5 via triggers automatically; brains stay outside FTS5 in V1 per PMD-9), formal metric trigger for V2 reopening (≤7/10 fixture queries return relevant hit → escalate sequentially to column weights → per-surface fusion → vector retrieval). Phase 2 (`memory-search-fts5`) now unblocked; @dev can start at will.
- Active-learning-loop Phase 1 (telemetry-foundation) shipped 2026-05-14: `src/learning-loop-migration.js` (idempotent migration runner — Phase 1 step adds partial index `idx_execution_events_context_load`), wired into `src/runtime-store.js` end-of-`ensureLegacyColumns`. New `appendContextLoadEvent(db, options)` helper exported from runtime-store (insert direto em `execution_events` com `run_key` nullable; valida `event_type` ∈ `{rule_loaded, brain_loaded}`). `src/commands/context-load.js` implementa `aioson context:load --target=<rule|brain>:<slug> --agent=<name> [--batch=...] [--feature=<slug>] [--classification=<lvl>] [--verbose] [--json]` (tier-1 silent, warn-not-fail validation, batch numa transação, forward-slash paths EC-ALL-13, payload clamp 4KB BR-ALL-08). Registrado em `src/cli.js` (KNOWN_COMMANDS + dispatch + help) e em `template/.aioson/config/autonomy-protocol.json` `tier1_silent`. i18n keys novas em 4 locales (en/pt-BR/es/fr). `tests/telemetry-foundation.test.js` cobre AC-ALL-101..105 + batch + warn-not-fail + entrada inválida (10/10 verde). Gate C CLI bloqueio foi sobrescrito conforme RDA-04 (manifest Sheldon é a fonte autoritativa); decisão documentada em Agent Trail do spec.
- Sub-task scout Phase 3 (wiring-and-lifecycle) shipped 2026-05-13 — **feature complete**. `.aioson/agents/deyvin.md` + template (byte-identical, 13611 bytes under 15360 budget) carry a new "Sub-task scout invocation" section with CLI path + CLI-less fallback subsections, including per-harness invocation examples for Claude Code Agent tool and Codex MultiAgentV2; the rubric row that previously said "deferred" now references `aioson scout:prep`. `src/commands/feature-close.js` gained an archival hook that copies scouts whose `feature_slug` matches the closing slug to `.aioson/context/features/{slug}/scouts/`, auto-appends a bullet to that feature's dossier under `## Sub-task scouts` (idempotent by id), and emits `runtime:emit type=sub_task action=archived_on_close`. `src/commands/memory.js > runMemorySummary` now always emits a "Scouts dispatched: N (top topics: ...)" row derived from `agent_events`, making scout activity visible to cold-load agent bootstrap. `src/doctor.js` gained the `scouts_directory_pruning` advisory check (severity warning) with `--fix` that deletes unattached scouts older than `prune_unattached_after_days` (default 90d) — attached scouts (with `feature_slug`) are NEVER pruned regardless of age, preserving cold-load memory artifacts. `src/dossier/scout-section.js` is the new pure helper for dossier appendage with idempotent `appendScoutToFeatureDossier`. New tests: `tests/deyvin-scout-wiring.test.js`, `tests/scout-section.test.js`, `tests/feature-close-scouts-archival.test.js` — 19/19 pass. Full regression post-feature: **2257/2294 (same 37 pre-existing failures preserved; zero new regressions)**. Total scout-feature footprint: 80 tests across 4 new files + 24 phase ACs all green + 14 pre-made decisions honored. Feature is ready for `@qa` Gate D.

- `tests/sync-agents-preflight.test.js` updated for Phase 6 active-learning-loop check coexistence (2026-05-14, `test-update-001` from the @tester triage): the two failing tests (146 + 194) stubbed `dossierTelemetry.emitDossierEvent` and counted raw call totals, but the new `learning_loop_template_parity_violation` check (added by Phase 6) fires extra emits when the synthetic tmpdir doesn't replicate the new template artifacts. Test 146 now filters `calls` by `payload.type === 'sync_agents_parity_violation'` before counting; all other assertions preserved. Test 194 was rescoped and renamed — the original asserted both "no parity_violation emit" AND "main() exit code 0", but the global exit code now reflects multiple parity checks beyond agent-chain (template, future additions). The renamed test "main() does not emit sync_agents_parity_violation when agent-chain parity is clean" keeps only the agent-chain-specific invariant. Both fixes are test-side; production code is correct as-is.

- Cross-platform path output normalization for user-facing fields (fix shipped 2026-05-14 from @tester `bug-found-006`): `src/squad/agent-teams-adapter.js > executorToTeammate` and `src/commands/learning-auto-promote.js > promoted_items[].file` now normalize `path.join` output to forward-slash before returning. These fields are emitted in JSON output, CLI logs, and consumed by downstream automation — without normalization, JSON consumers would see `.aioson\\rules\\foo.md` on Windows and `.aioson/rules/foo.md` on POSIX, an interop problem for any tool that parses or matches against the field. Fix is one line per site: `(...).replace(/\\\\/g, '/')` after the `path.join`. Decision: normalize at the production boundary, not in tests, because the tests' forward-slash expectation matched the cross-platform contract. Node's fs APIs accept forward-slash on Windows so OS-level path handling is unaffected. Recovers 2 real singletons in the @tester `real-assertion-failure-singleton` bucket; the third entry in that bucket (`live-command.test.js:308`) was misclassified by the triage and was already fixed by bug-005 (state.json not written → validation read empty state → no rejection). agent-teams-adapter 36/36 + learning-auto-promote 10/10 green. Full suite: 2414/2422 (was 2413/2422). Remaining 8 failures are 6 non-@dev decisions (@architect kernel-size, @architect QA-PERF flake) + investigation (docs/en/schemas archaeology) + @tester refresh (sync-agents-preflight Phase 6 check).

- Live session FS layout now works on Windows (fix shipped 2026-05-14 from @tester `bug-found-005`): `src/commands/live.js` introduces `sessionKeyToDirName(key)` which replaces `:` with `__` when converting a session key to a filesystem directory name, and `resolveLivePaths` applies it before every `path.join`. NTFS reserves `:` in filenames (drive-letter separator + ADS syntax), so `mkdir 'direct-session:123:deyvin'` failed with ENOENT on Windows — and the `writeLiveState`/`appendLiveEvent`/`writeLiveSummary` catches swallowed the error silently, leaving `state.json`/`events.ndjson`/`summary.md` never written. ALL 6 failures in the @tester triage's `real-assertion-failure-live-cluster` bucket stemmed from this single bug, with different symptoms (direct ENOENT × 2, back-to-back validation that reads state.json failing to fire, live:list returning empty, runtime status child_task_count miscount, summary.md path mismatch). The feature has been silently broken on Windows since commit `4621cf3` (feat: add tracked live sessions and runtime projections); CI runs on Linux where colons are valid filenames so it never surfaced. The session_key stored in SQLite and returned by the CLI keeps its colon-separated public format — only the on-disk directory name is sanitized. Helper is exported for callers (including tests) that need to resolve paths from a session key. live-command 8/8 + live-json-output 2/2 + runtime-command relevant test green. Full suite: 2413/2422 (was 2407/2422). +6 net recovered.

- `aioson context:health` now releases SQLite WAL siblings cleanly on Windows (fix shipped 2026-05-14 from @tester `bug-found-004`): `src/commands/context-health.js` runs `db.pragma('wal_checkpoint(TRUNCATE)')` before `db.close()`. Without the checkpoint, the `aios.sqlite-wal` and `aios.sqlite-shm` sibling files linger ~50-100ms after close on NTFS, long enough to make any subsequent `fs.rm` of the parent directory fail with EBUSY. Refined diagnosis vs the @tester triage: this was NOT the handle-discard pattern of bug-002 — both the test and the production code closed handles correctly, the leak was the WAL sibling files specifically. The matching test-side change uses `cleanupTmpDir` as a safety net in case future code paths re-introduce the issue. Recovers 6/7 context-health tests (the 7th was always passing — short-circuit on missing context dir before any DB is opened). Same WAL pattern likely affects `tests/context-search.test.js:15` (observed ENOTEMPTY in `search/` subdir during this session) and may recur in other tests that open WAL-mode SQLite — apply the same recipe when surfaced.

- `aioson squad:export` now works on Windows (fix shipped 2026-05-14 from @tester `bug-found-003`): `src/commands/squad-export.js` converts the `-C` and `-czf` path arguments to forward-slash and, on `process.platform === 'win32'`, prepends `--force-local` to the tar args. Background: GNU tar treats any `HOST:PATH` argument as a remote-host reference, so `-C C:\dev\foo` was triggering `tar: Cannot connect to C: resolve failed`. Verified that forward-slash alone doesn't fix it (the colon still triggers remote-host parsing) and `--force-local` alone mangles backslashes through the msys layer — both are required. Linux/macOS unaffected: paths have no colons, and `--force-local` is gated on win32 only. Tests `tests/squad-export.test.js` (3/3) and `tests/tool-invocation-hardening.test.js#SF-12` go from RED to GREEN. Full suite: 2402/2422 (was 2399/2422).

- Squad test cluster EBUSY recovery on Windows (fix shipped 2026-05-14 from @tester `bug-found-002`): new test-infra helper `tests/helpers/sqlite-cleanup.js` (~30 LOC) exports `closeHandles(...items)` (swallow-errors) and `cleanupTmpDir(tmpDir, { handles: [...] })` which closes any tracked better-sqlite3 handles then calls `fs.rm` with `{ force: true, maxRetries: 5, retryDelay: 100 }` — the maxRetries window bridges the ~50-100ms between `db.close()` returning and NTFS releasing the OS-level file lock. 5 test files refactored (`squad-{daemon,dashboard,score,api-endpoints,webhook-production}.test.js`) to capture `await openRuntimeDb(tmpDir)` into a `handle` variable and pass it through the helper at teardown. **Production bug discovered during fix**: `src/commands/squad-score.js:284` called `openRuntimeDb(projectDir)` without `await` inside an async function — the destructure unpacked the Promise (db === undefined), the try block threw on `db.prepare`, the catch silenced it, AND the underlying Promise still resolved and leaked a never-closed handle. Single-line fix: added `await` + a comment explaining the leak. This means `runSquadScore` was silently never persisting scores to the runtime DB since the feature was written — historic score tracking was broken without anyone noticing. Squad cluster recovery: 100/100 green (was 82/100). Full suite: 2399/2422 green (was 2382/2422 after bug-001). Remaining 23 failures are separate clusters (context-health, live-*, json-schema, sync-agents-preflight) outside bug-002 scope.

- `aioson install --dry-run` now emits a visually-distinct preview (fix shipped 2026-05-14 from @tester `bug-found-001`): new branch `else if (dryRun)` in `src/commands/install.js` plus 4 new i18n keys (`install.dry_run_header`, `install.dry_run_done_at`, `install.dry_run_files_copied`, `install.dry_run_files_skipped`) in en/pt-BR/es/fr. Output now leads with `⚠  DRY RUN — no files were written.` followed by `Files that would be copied: N` / `Files that would be skipped: N` (conditional verb form). Non-en locales include the english parenthetical (`would be copied` / `would be skipped`) for cross-locale grep in ops logs. Real-install path unchanged. New `tests/install-dry-run.test.js` pins AC-DRY-01 (visible marker), AC-DRY-02 (conditional verbs + absence of past-tense summary lines), AC-DRY-03 (zero filesystem mutations) — 3/3 green. Full suite delta vs baseline: 2382/2422 pass, 40 fail (was 2377/2419 pass, 42 fail — zero new failures introduced).

- Genomes `copywriting` and `copywriting-brunson` migrated to **Track 4.2 modular structure** on 2026-05-14 (commits `9c47294` + `76def2f`): each genome now ships as a folder with `SKILL.md` + `manifest.json` + `references/` subtree. Common `references/` items in both: `frameworks/`, `methodology.md`, `decision-weights.md`, `meta-axioms.md`, `application-notes.md`. Genome-specific extras: standard `copywriting` adds `heuristics.md`; `copywriting-brunson` adds `evidence-and-attribution.md`. Brunson `frameworks/` covers 6-part-structure, origin-story, perfect-webinar-script, persuasive-storytelling-5-structures, value-stack, who-what-why-how; standard `copywriting` `frameworks/` covers 5-act-narrative, classical-formulas, offer-stack, one-belief, pms-research, two-paths-close. Legacy single-file `copywriting.md` + `copywriting.meta.json` retained as fallback so G0 genome-resolution rule (folder-preferred, file-fallback) keeps working for downstream projects on older AIOSON versions. Mirror in `template/.aioson/genomes/` is byte-identical (workspace ↔ template parity). `.gitignore` rule for `references/` scoped to project root (commit `f83dd4c`) to avoid collateral ignores of nested `references/` paths inside genomes.

## What the system does not have yet

These are real gaps, not already-delivered work:

- hard enforcement of isolated write scopes inside the edit/execution harness itself
- real filesystem/worktree isolation per lane during implementation

## Correct reading of the backlog

The outdated assumption was:

- "next step is updating prompts so agents can call CLI"

The corrected reading is:

- prompt awareness is already delivered
- the next work is operational hardening of autonomy

## Priority order discovered in this session

1. clean old planning docs so they stop describing completed work as pending
2. close the sequential autonomous flow
3. revisit runner depth and then parallelism

## Canonical planning files for this topic

- `products-features/upgrade-agents/agentes-autonomos-cli.md`
- `products-features/upgrade-agents/roadmap-consolidado-autonomia-orquestracao.md`
- `products-features/upgrade-agents/checklist-executavel-autonomia-orquestracao.md`
- `plans/Upgrade-Agents/Plano-Definitivo-Implementacao-Protocol-Contracts.md`

## Practical resume point

If a future agent resumes this topic, the safest next implementation slice is:

- move from preflight `parallel:guard` validation to enforced write blocking inside execution/edit flows

After that:

- evaluate whether parallel lanes need worktree-backed isolation instead of shared-workspace coordination
