---
last_updated: 2026-05-22
active_feature: neural-chain
active_phase: 1
next_step: "Feature DONE post hotfix v1.17.2 — security hardening. @pentester adversarial review post v1.17.1 surfaced 3 findings (SF-NC-01 HIGH block / SF-NC-02 MEDIUM review / SF-NC-03 LOW note); todos fixed em v1.17.2: SF-NC-01 + SF-NC-02 via novo src/neural-chain-sanitize.js#isUnsafePath wired em 3 boundaries (Layer A flattenAudits + Layer B deriveSessionPairs/computeCoEditPairs + CLI runChainAudit); SF-NC-03 via normalizeThreshold rejeitando -0 + trust-boundary note em requirements EC-NC-07. security-findings.json status=fixed em todos; @qa é final decision owner. Re-pentester probe recomendado pra confirmar mitigação. AC-AUDIT-NC 7/7 still satisfied. Next: npm publish manual v1.17.2 (supersedes v1.17.0 + v1.17.1 que foram tagged mas não published). Próxima feature: /product."
status: done
---

# Dev State

**Feature:** neural-chain
**Phase:** 1
**Status:** done (security hotfix v1.17.2 shipped 2026-05-22)
**Next step:** npm publish manual v1.17.2. Próxima feature: /product. (1) Criar src/neural-chain-noise-file.js: exporta writeNoiseFile({featureSlug, audits, autonomyMode='guarded', now}) que gera path .aioson/context/noises/{feature-slug}-{YYYYMMDD-HHMM}.md (ou unspecified-{ts}.md fallback quando featureSlug==null) com frontmatter YAML {slug, edit_at, autonomy_mode, source_files, total_items, resolved_items} + body markdown com checkboxes '- [ ] {target_path} — {motivo (edge_type + confidence)}'. Items NÃO usam :symbol em M1 (BR-NC-09 file-level). (2) Exporta readNoiseFileAndRecompute({path}) que parsea YAML+body, recomputa resolved_items contando '- [x]', retorna {frontmatter, items[], allResolved}. Lazy — sem file watcher. (3) Exporta maybeDeleteNoiseFile({path}) que invoca readNoiseFileAndRecompute; se allResolved OR body sem items pending → fs.unlink + catch ENOENT (idempotent EC-NC-10). (4) Integrar runChainHookOnAgentDone em src/neural-chain-agent-ingest.js: APÓS audits collect, se algum audit tem impacts_found > 0 AND autonomy='guarded' → writeNoiseFile com todos audits flattened. Modo 'standard'/'autonomous' fica pra Slice 6 (precisa threshold rules pra decidir auto-fixable vs noise). (5) Tests novos cobrindo: write format frontmatter+body shape, multi-file aggregation no mesmo session file, lazy recompute do resolved_items quando re-read, deletion-on-close trigger automático, EC-NC-09 corrupted frontmatter re-cria limpo, EC-NC-10 race delete idempotent unlink, fallback unspecified path quando featureSlug=null. (6) Verify zero novas regressões via npm test. AC-AUDIT-NC item 2 progress (parcial — @neo surfa ainda pendente até Slice 5). Próximos: Slice 5 @neo activation step (template parity sheldon-001) + Slice 6 autonomy threshold + chain_auto_threshold config.

## Context package

1. project.context.md
2. spec-neural-chain.md
3. requirements-neural-chain.md
4. sheldon-enrichment-neural-chain.md

## History

- 2026-05-21: phase 1 — Phase 1 Slice 2: chain:audit CLI command + git ingest. (1) Criar src/commands/chain-audit.js implementando 'aioson chain:audit <file> [--feature=<slug>] [--json]' que: lê chain_edges WHERE end_at IS NULL AND source_path = <file> ORDER BY confidence DESC LIMIT N (top-N tuneável, default 20); retorna lista de impactos com (target_path, edge_type, confidence, hit_count, last_seen_at). (2) Criar helper src/neural-chain-git-ingest.js que executa 'git log --pretty=format:%H --name-only -n 1000 -- HEAD' (bounded a últimos 1000 commits per pre-decision sheldon I1), parsa pares de arquivos co-edited no mesmo commit, e popula chain_edges com edge_type='git_co_edit' aplicando BR-NC-01 (min(1.0, count/10)) + BR-NC-08 hard cap 10k via archive. (3) Registrar 'chain:audit' em src/cli.js KNOWN_COMMANDS + dispatch + help line. (4) i18n keys novas em 4 locales (en, pt-BR, es, fr) — help_chain_audit + chain_audit.* output messages. (5) Tests cobrindo: audit retorna lista vazia em DB fresca, audit retorna edges ordenadas por confidence, git ingest popula edges a partir de log fixture, cap 10k archiva oldest, idempotency (re-ingest same commits não duplica). Brain dev/patterns sheldon-005 CLI-first integration aplica. ECs cobertos no spec: EC-NC-06 sem git history skip + EC-NC-03 file never-seen first ingest. Slice 3 depois = agent_event ingest hook em runAgentDone.
- 2026-05-21: phase 1 — Phase 1 Slice 3: agent_event ingest hook em runAgentDone. (1) Criar src/neural-chain-agent-ingest.js (pure helper) que lê agent_event rows da sessão corrente, deriva co-edit pairs dos source_files do mesmo agent_event ou agrupados por run_key/agent_name na mesma janela temporal curta, e popula chain_edges com edge_type='agent_event' aplicando BR-NC-01 (min(1.0, count_last_30d / 5) — saturação 5 em janela 30d) + BR-NC-08 hard cap 10k via archive oldest. Reusar pattern de ingestGitCoEditEdges (nested Map<source, Map<target, ...>> + UPSERT ON CONFLICT respeitando partial UNIQUE index). (2) Modificar src/commands/runtime.js função runAgentDone: APÓS telemetry write + integrity check, invocar agent_event ingest hook + chain:audit pra current source_file(s). EC-NC-05: se session não tem file edits (extrair source_files de agent_event payloads), skip no-op + ainda emit chain_audit event com impacts_found=0. (3) Tests cobrindo: helper isolation (insere agent_event fixtures, valida ingest derives correct pairs), runtime integration (agent:done call com edits → chain_edges populated + chain_audit event emitted; agent:done sem edits → skip path + chain_audit ainda emit). (4) Verify zero novas regressões via npm test. Próximos slices Phase 1: Slice 4 noise file write/lifecycle BR-NC-06 (path {feature-slug}-{YYYYMMDD-HHMM}.md + frontmatter YAML + checkboxes + lazy resolved_items recompute + deletion-on-close); Slice 5 @neo activation step blocker (sheldon-001 template parity); Slice 6 autonomy threshold rules BR-NC-02/03 + chain_auto_threshold config field.
- 2026-05-21: phase 1 — Phase 1 Slice 4 LANDED: noise file write/lifecycle (BR-NC-06). Novos: src/neural-chain-noise-file.js (~225 LOC, writeNoiseFile + readNoiseFileAndRecompute + maybeDeleteNoiseFile + helpers, sync fs) + tests/neural-chain-noise-file.test.js (13/13 verde, 296ms). Modificados: src/neural-chain-agent-ingest.js (runChainHookOnAgentDone recebe targetDir+autonomyMode; refator pass1/pass2 pra injetar noise_file payload em telemetry) + src/commands/runtime.js (targetDir passado em 2 call sites). Regressão: 2746/2743 + 1 skipped + 2 fail (AC-P1-07 pré-existente + security-audit ENOTEMPTY Windows flake, isolated PASS). EC-NC-09 + EC-NC-10 cobertos. BR-NC-09 file-level only verificado via regex anti-:symbol. Modo guarded escreve noise file quando hasAnyImpacts; standard/autonomous skip (Slice 6). AC-AUDIT-NC item 2 parcial — @neo surfa ainda pendente em Slice 5. Próximo: Slice 5 @neo activation step (template parity sheldon-001) + Slice 6 autonomy threshold reading + chain_auto_threshold config.
- 2026-05-21: phase 1 — Phase 1 Slice 5 LANDED: @neo noise blocker step (prompt-only). Files modificados: .aioson/agents/neo.md (4 edits — Step 1 scan row + Step 1.5 BLOCKER block + Step 3 stage table top-row + dashboard template line) + template/.aioson/agents/neo.md (cp mirror byte-a-byte, diff -q PARITY_OK). Sem código em src/. Regressão: 2746/2744 + 1 skipped + 1 fail (AC-P1-07 pré-existente). AC-AUDIT-NC item 2 ✓ satisfeito + item 7 (template parity) ✓ verificado. Detection via regex inline `^- \[ \]` ou readNoiseFileAndRecompute helper. BLOCKER stage tem precedência total na tabela de stages. Resolução = marcar `- [x]` (lazy deletion via próximo hook); explicit skip = "skip noises" + reason no routing block. Próximo: Slice 6 autonomy mode wiring + threshold rules BR-NC-02/03.
- 2026-05-21: phase 1 — Phase 1 Slice 6 LANDED: autonomy mode wiring + threshold rules BR-NC-02/03. Novos: src/neural-chain-config.js (readChainConfig + normalize* helpers + VALID_AUTONOMY_MODES constants, EC-NC-07 honrado em 4 caminhos) + tests/neural-chain-autonomy.test.js (23/23 verde, 448ms). Modificados: src/neural-chain-noise-file.js (serializeItem + flattenAudits + parseItems propagam marker opcional, regex estende pra capturar [MARKER] entre checkbox e target_path) + src/neural-chain-agent-ingest.js (require config + path; novos helpers escapeRegex + isTestFileFor pra BR-NC-02 rule a; novo classifyImpact aplicando rules a + c — rule b deferida; runChainHookOnAgentDone signature muda autonomyMode='guarded' → null sentinel + novo chainAutoThreshold; auto-resolve via readChainConfig quando targetDir presente; modos standard/autonomous AGORA escrevem noise file com items prefixados [AUTO-FIXABLE] ou [AUTO-FIXABLE-BEST-EFFORT]; auto_fixable_count computado por audit + total session-level; telemetry payload ganha auto_fixable_count + chain_auto_threshold). Decisões: BR-NC-02 rule (b) literal identifier match diferida (requer diff parsing pesado, follow-up M1.5/M2); .aioson/config.md frontmatter como source (per spec); null sentinel mantém backward-compat Slice 3+4 tests; classifier in-place mutation pra writeNoiseFile permanecer pura; marker regex limit [A-Z][A-Z0-9_-]* anti-injection. Update test antigo Slice 4 "(standard/autonomous skips)" → reescrito pra Slice 6 ("writes noise file with mode in frontmatter"). Regressão: 2769/2767 + 1 skipped + 1 fail (AC-P1-07 pré-existente). +23 tests novos. AC-AUDIT-NC item 3 ✓ satisfeito. Resta apenas item 6 (CHANGELOG) pra closing. Próximo: closing tasks Phase 1 — CHANGELOG.md [1.17.0] + version bump + Gate D QA.
- 2026-05-21: phase 1 — Phase 1 closing LANDED: CHANGELOG [1.17.0] + version bump 1.16.0→1.17.0 + Gate D QA PASS (AC-AUDIT-NC 7/7 verificados, perf p95=0.085ms @ 10k edges, security audit 0 findings, 2 Medium residual M-01+M-02 doc'd, auto-cycle não triggered). spec gate_d=approved + features.md done (2026-05-21 completed). Commits 920e02a (release) + 097eb16 (Gate D sign-off) + tag v1.17.0.
- 2026-05-22: hotfix — Hotfix v1.17.1 LANDED: M-01+M-02+M-003 consolidados. Novos: src/neural-chain-telemetry.js (shared emitter — buildChainAuditPayload + emitChainAuditEvent + REQUIRED_FIELDS, schema completo BR-NC-10 8 fields com source_files plural + tokens_used=0 placeholder + EC-NC-05 no-op duration_ms=0+error=null defaults). Modificados: src/neural-chain-agent-ingest.js (queryImpacts SQL com ROW_NUMBER window function PARTITION BY target_path pra BR-NC-01 dedupe + 2 call sites do emit migrados pro helper compartilhado, legacy source_file singular alias preservada backward-compat) + src/commands/chain-audit.js (mesma SQL window function + emit migrado + source_files=[filePath] singleton array + legacy source_file alias) + tests/neural-chain-invariants.test.js (REQUIRED_BR_NC_10_FIELDS promovido pra 8 fields + 2 dedupe tests novos cobrindo hook + CLI dual-source case) + spec-neural-chain.md EC-NC-04 amendment V1 acceptable deviation + requirements-neural-chain.md EC-NC-04 amendment. Decisões: M-01 amend (no code, BR-NC-11 non-blocking IS load-bearing, withRetry deferido M1.5/M2); window function vs subquery (SQLite 3.45+ disponível, semântica cleaner); legacy source_file alias preservado pra dashboard queries v1.17.0 (will remove v2). Regressão: 2775/2772 + 1 skipped + 2 fail (AC-P1-07 pré-existente + AC-ALL-101 flake intermitente, ambos não relacionados). +2 tests dedupe. Próximo: npm publish manual v1.17.1 (supersedes v1.17.0 que não foi published).
